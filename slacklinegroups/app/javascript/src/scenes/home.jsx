import React from 'react'

import MenuSearchBar from '../components/nav/MenuSearchBar';

const Home = props => {
    return (
      <React.Fragment>
        <MenuSearchBar />
      </React.Fragment>
    );
};

export default Home;